# دورهٔ آموزشی: از صفر تا مقاله — LC-Intention-NAS

این دوره یک **وب‌سایت** است: از [index.html](index.html) شروع کنید (به‌صورت
محلی باز کنید، یا بعد از فعال‌کردن GitHub Pages از آدرس آنلاین).

- زبان: فارسی ساده، اصطلاح‌های فنی به English.
- هر درس: قصه → دقیق‌تر → در پروژهٔ ما → آزمونک 🎯 (با نمودار و دیاگرام).
- هر عددی که در دوره می‌بینید از اجراهای واقعی همین repository آمده است
  (`logs/experiments.jsonl`).

## وضعیت درس‌ها

| # | درس | وضعیت |
|---|-----|-------|
| ۰۰–۰۴ | نقشهٔ راه، مسئله، داده، Neural Network، CNN یک‌بعدی | ✅ آماده |
| ۰۵–۱۱ | Training، Validation، NAS، µNAS، Quantization، STM32، مقاله‌نویسی | 🔜 همراه با پیشرفت پروژه |

## برای توسعه‌دهنده

صفحهٔ نمونه (قالب مرجع): `00-roadmap.html`. استایل و موتور آزمونک:
`assets/course.css` و `assets/course.js`. درس جدید = کپی ساختار صفحهٔ نمونه +
افزودن کارت آن در `index.html`.
