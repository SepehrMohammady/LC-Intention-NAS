# Data (large, not versioned — see README for how to obtain)
datasets/*/data/
Materials/
# External µNAS fork checkout (unlicensed upstream; kept out of our MIT tree)
Materials/uNAS-elios/

# Python
.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/

# Experiment artifacts (large)
runs/
checkpoints/
*.pth
*.onnx
!models/best/*.onnx

# LaTeX build (main.pdf IS committed — rebuilt after every .tex change)
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.synctex.gz
paper/*.fdb_latexmk
paper/*.fls

# OS / IDE
.DS_Store
Thumbs.db
.vscode/

# Kept local, not published to GitHub (user decision 2026-07-28)
course/
paper/

# Personal working file, not published
checklist.html

# QUB collaboration material (drafts, third-party PDFs)
qub/

# Local course entry point (course/ itself is local-only)
index.html

# Agent working notes, kept local
CLAUDE.md
